// TODO: risk card for interaction result
// Shows:
// - severity badge (low=green / moderate=amber / high=red)
// - drug_a + drug_b names
// - llm_summary paragraph
// - mechanism + clinical_effect + recommendation fields
// - sources list (linked)
// - disclaimer text (always shown)

export function RiskCard({ result }: { result: any }) {
  // TODO: implement
  return <div />
}
